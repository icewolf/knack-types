stages:
  - release

before_script:
  - yarn install

publish:
  image: node:14
  stage: release
  cache:
    paths:
      - .yarn
  script:
    - yarn build
    - npx semantic-release
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
